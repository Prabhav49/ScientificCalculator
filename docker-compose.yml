version: '3.8'

services:
  calculator:
    image: scientific-calculator
    container_name: scientific_calculator
    stdin_open: true  # Keep STDIN open for interactive input (useful if needed later)
    tty: true         # Allocate a pseudo-TTY
    entrypoint: ["java", "-jar", "app.jar"]
